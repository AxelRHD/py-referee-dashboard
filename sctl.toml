[tasks.logs]
host = "mimir"
steps = [
    "docker logs --tail 50 referee-dashboard",
]

[tasks.status]
host = "mimir"
steps = [
    "docker ps --filter name=referee-dashboard --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'",
]
